# 使用方式：
# 1. 复制本文件为 `.env.local`。
# 2. 只在 `.env.local` 中填写真实密钥；不要把 `.env.local` 提交到 Git。
# 3. 前端变量以 `VITE_` 开头，后端变量由 `backend/server.mjs` 读取。

# Gemini 模型 API Key。
# 是否必需：可选。未配置时工作台和 Run Lab 会降级为“仅编译预览”，不会真实调用模型。
# 获取方式：登录 Google AI Studio 或 Google Cloud，对应项目中创建 Gemini API Key。
# 示例：GEMINI_API_KEY=AIza...（不要把真实值写进本文件）
GEMINI_API_KEY=

# 本地 Node API 监听端口。
# 是否必需：可选。默认 8787；如端口被占用，可改成其他本地端口。
# 获取方式：自行选择未被占用的本机端口。
API_PORT=8787

# 前端访问本地 Node API 的基础地址。
# 是否必需：可选。默认 http://127.0.0.1:8787；如果修改 API_PORT，需要同步修改这里。
# 获取方式：由 API_PORT 组合得到，例如 API_PORT=18787 时填写 http://127.0.0.1:18787。
VITE_API_BASE_URL=http://127.0.0.1:8787

# 未来模型供应商 Key 预留。
# 是否必需：可选。当前版本不读取这些变量，只作为后续接入 OpenAI/Claude/本地模型时的占位说明。
# 获取方式：到对应模型服务商控制台创建；启用前不要填写真实值。
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
